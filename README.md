# Nickson Kaigi - Resume 

## To compile


- run `aspell` to check and replace spelling mistakes

        $ aspell -t -c main.tex
 
 - run `xelatex`

        $ xelatex main.tex
 

A `PDF` file will have been in the same directory as `main.tex`
